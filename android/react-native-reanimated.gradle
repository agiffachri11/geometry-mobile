project.ext.react = [
    enableHermes: true
]

apply from: new File(["node", "--print", "require.resolve('react-native/package.json')"].execute(null, rootProject.projectDir).text.trim(), "../react.gradle")

android {
    sourceSets {
        main {
            res.srcDirs += ['src/main/res']
        }
    }
}